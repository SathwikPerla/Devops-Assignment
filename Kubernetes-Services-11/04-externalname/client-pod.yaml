apiVersion: v1
kind: Pod
metadata:
  name: dns-test-client
spec:
  containers:
    - name: client
      image: alpine:latest
      command: ["sh", "-c", "apk add --no-cache curl bind-tools && sleep 3600"]
